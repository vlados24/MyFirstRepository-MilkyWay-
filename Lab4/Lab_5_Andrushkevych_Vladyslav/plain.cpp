#include "pch.h"
#include "plain.h"

using namespace std;

int main(int args, const char * argv[]) {

	Plain plain;
	plain.Plain2(500, "eagle", 105);
	plain.Plain3(700, "Gangsta", 120);

	system("pause");
	return 0;
}